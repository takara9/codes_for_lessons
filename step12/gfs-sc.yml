apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "gluster-heketi"
provisioner: kubernetes.io/glusterfs
parameters:
  resturl: "http://172.20.1.20:8080"
  restuser: "admin"
  restuserkey: "admin"
